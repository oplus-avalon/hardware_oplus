package com.oplus.widget;

import android.content.Context;
import android.util.AttributeSet;
import android.view.View;

import java.util.List;

/**
 * Class-closure stub for com.oplus.widget.OplusMenuView.
 *
 * Real class (OOS): public class OplusMenuView extends android.view.View.
 * Gallery references: <init>(Context, AttributeSet) and setItem(Ljava/util/List;)V.
 * The extra (Context) and (Context, AttributeSet, int) constructors mirror the
 * OOS class and keep the View super-chain complete; raw List param matches the
 * erased Ljava/util/List; descriptor the gallery invokes.
 */
public class OplusMenuView extends View {

    public OplusMenuView(Context context) {
        super(context);
    }

    public OplusMenuView(Context context, AttributeSet attrs) {
        super(context, attrs);
    }

    public OplusMenuView(Context context, AttributeSet attrs, int defStyleAttr) {
        super(context, attrs, defStyleAttr);
    }

    public void setItem(List items) {
    }
}
