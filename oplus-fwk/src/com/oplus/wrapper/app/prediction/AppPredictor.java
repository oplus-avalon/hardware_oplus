package com.oplus.wrapper.app.prediction;

/**
 * Closure companion stub. Referenced by OppoGallery2 only as a bare type
 * (return of AppPredictorFactory.create and a ShortcutLoader ctor parameter);
 * no members are invoked, so an empty class closes the reference.
 */
public class AppPredictor {
}
