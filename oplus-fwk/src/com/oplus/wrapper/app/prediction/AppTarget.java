package com.oplus.wrapper.app.prediction;

/**
 * Closure companion stub. Referenced by OppoGallery2 only as a bare type
 * (a newSelectableTargetInfo parameter); no members are invoked, so an empty
 * class closes the reference.
 */
public class AppTarget {
}
